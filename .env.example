# Local development only. Copy variable names into an uncommitted .env file and set a
# unique local password. Never reuse production credentials.
POSTGRES_DB=st_online_dev
POSTGRES_USER=st_online_dev
POSTGRES_PASSWORD=
POSTGRES_PORT=5432

